.devenv/
.direnv/
*.iso
result
# Cockpit's pkgs/lib is vendored by Nix at build time (package.nix postPatch);
# a local copy may exist for editor/tsc runs but must not be committed.
pkgs/cockpit-router/pkgs/
__pycache__/
*.pyc
