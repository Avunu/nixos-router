.devenv/
.direnv/
*.iso
result
# Cockpit's pkg/lib is vendored by Nix at build time (package.nix postPatch);
# a local copy may exist for editor/tsc runs but must not be committed.
pkg/cockpit-router/pkg/
__pycache__/
*.pyc
