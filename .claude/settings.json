{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v node >/dev/null 2>&1; then node \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/validate-edit.js\"; fi"
          }
        ]
      }
    ]
  }
}
